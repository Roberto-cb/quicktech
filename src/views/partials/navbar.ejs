<header class="site-header">
  <!-- Logo -->
  <div class="logo">
    <a href="/" class="logo-link">QuickTech</a>
  </div>

  <!-- Buscador + Carrito (carrito a la derecha del buscador) -->
  <div class="search-and-cart">
    <form method="GET" action="/" class="search-form" role="search">
      <input
        type="text"
        name="q"
        placeholder="Buscar productos…"
        value="<%= typeof q !== 'undefined' ? q : '' %>"
        class="search-input"
        aria-label="Buscar productos"
      />
      <button type="submit" class="btn search-btn">Buscar</button>
    </form>

    <a href="#" class="cart-btn" id="navbar-cart-link" aria-label="Carrito">
      <span class="cart-icon" aria-hidden="true">🛒</span>
      <span class="cart-count" id="cart-count" hidden>0</span>
    </a>
  </div>

  <!-- Navegación -->
  <nav class="navbar" aria-label="Navegación principal">
    <% if (typeof user !== 'undefined' && user) { %>
      <a href="/profile" class="nav-link">Perfil</a>
      <form method="POST" action="/auth/logout" style="display:inline;">
        <button type="submit" class="btn btn-ghost">Cerrar sesión</button>
      </form>
    <% } else { %>
      <a href="/auth/register" class="nav-link">Registrarse</a>
      <a href="/auth/login" class="nav-link">Iniciar sesión</a>
    <% } %>
  </nav>
</header>